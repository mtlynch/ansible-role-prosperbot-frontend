---
sudo: required
env:
  - distro: ubuntu1404
    init: /sbin/init

services:
  - docker

install:
  - pip install ansible-lint==3.3.3

script:
  - ./build

notifications:
  webhooks: https://galaxy.ansible.com/api/v1/notifications/
